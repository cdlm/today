source 'https://rubygems.org'

gemspec

group :development do
  # a few optional addons to guard...
  gem 'pry', :require => false
  # linux-specific:
  gem 'rb-inotify', :require => false
  # mac-specific
  gem 'rb-fsevent', :require => false
  gem 'terminal-notifier-guard', :require => false
end
